[workspace]
authors = ["yunyun <dana.yiyun@gmail.com>"]
channels = ["conda-forge"]
name = "comp4462-project"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
# dep = "sudo apt-get update && sudo apt-get install -y texlive-xetex texlive-fonts-recommended texlive-plain-generic"
# start = "python asgn/proj1phase1/app.py"
register-kernel = 'python -m ipykernel install --user --name "pixi-env" --display-name "COMP4462 Project Pixi Env"'

[system-requirements]
# cuda = "12.8"

[dependencies]
# Run `pixi install` to install the dependencies listed below.
# To add requirements, run `pixi add <package-name>` or `pixi add <package-name>@<version-specifier>`.
# Or, eg. pixi add -- --requirements HA07-LLM requirements.txt
# pixi add --pypi -- -r HA07-LLM/requirements.txt
# ipykernel = ">=6.0.0,<7"
# jupyterlab = ">=3.0.0,<4"
matplotlib = ">=3.10.8,<4"
numpy = ">=2.0.0,<3"
pandas = ">=2.0.3,<3"
pytorch = ">=2.10.0,<2.11"
scikit-learn = ">=1.3.2,<2"